local modkey = "Mod4"
local shift = "Shift"
local ctrl = "Control"

return {
    none = {},
    modkey = { modkey },
    shift = { shift },
    ctrl = { ctrl },
    mod_shift = { modkey, shift },
    mod_ctrl = { modkey, ctrl },
    mod_shift_ctrl = { modkey, shift, ctrl }
}
